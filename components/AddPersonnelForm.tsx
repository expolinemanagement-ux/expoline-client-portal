'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function AddPersonnelForm(){const router=useRouter();const [message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form).entries());const res=await fetch('/api/personnel',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const json=await res.json();setMessage(res.ok?'Personnel saved to PostgreSQL.':json.error||'Unable to save personnel.');if(res.ok){form.reset();router.push('/personnel');router.refresh();}}return <form className="formGrid" onSubmit={submit}><label>Full Name<input name="fullName" required placeholder="Full name"/></label><label>Chinese Name<input name="chineseName" placeholder="中文姓名（optional）"/></label><label>Company<select name="companyName" required><option value="">Select company</option><option>Demo Construction Company</option><option>Demo Hospitality Group</option><option>Demo Marine Services</option><option>Demo Resort Operations</option></select></label><label>Nationality<input name="nationality" placeholder="Nationality"/></label><label>Position<input name="position" placeholder="Position"/></label><label>Passport Number<input name="passportNumber" placeholder="TEST-PASSPORT only"/></label><label>Passport Expiry<input name="passportExpiry" type="date"/></label><label>Status<select name="status"><option>Candidate</option><option>Recruiting</option><option>Processing</option><option>Active</option><option>Inactive</option></select></label><div className="formActions"><button className="primary" type="submit">Save Personnel</button>{message&&<span className="success">{message}</span>}</div></form>