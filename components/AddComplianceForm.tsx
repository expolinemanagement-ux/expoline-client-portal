'use client';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';
export default function AddComplianceForm(){const router=useRouter();const [people,setPeople]=useState<any[]>([]);const [message,setMessage]=useState('');useEffect(()=>{fetch('/api/personnel').then(r=>r.json()).then(setPeople).catch(()=>setPeople([]))},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form).entries());const res=await fetch('/api/compliance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const json=await res.json();setMessage(res.ok?'Compliance record saved to PostgreSQL.':json.error||'Unable to save record.');if(res.ok){form.reset();router.push('/compliance');router.refresh()}}return <form className="formGrid" onSubmit={submit}><label>Personnel<select name="personnelId" required><option value="">Select personnel</option>{people.map(p=><option key={p.id} value={p.id}>{p.fullName} — {p.company.name}</option>)}</select></label><label>Record Type<select name="recordType"><option>Visa</option><option>Work Permit</option><option>Medical</option><option>Insurance</option></select></label><label>Reference Number<input name="referenceNumber" placeholder="TEST-VISA / TEST-PERMIT / TEST-POLICY"/></label><label>Issue / Completion Date<input name="issueDate" type="date"/></label><label>Expiry Date<input name="expiryDate" type="date"/></label><label>Status<select name="status"><option value="PENDING">Pending</option><option value="VALID">Valid</option><option value="EXPIRING_SOON">Expiring Soon</option><option value="EXPIRED">Expired</option><option value="NOT_REQUIRED">Not Required</option></select></label><label className="full">Notes<textarea name="notes" placeholder="Optional notes"/></label><div className="formActions"><button className="primary" type="submit">Save Record</button>{message&&<span className="success">{message}</span>}</div></form>