import LanguageToggle from './LanguageToggle';import {getCurrentUser} from '@/lib/auth';import {t} from '@/lib/i18n';export default async function Topbar({title}:{title?:string}){const user=await getCurrentUser();const tr=(key:string)=>t(user?.preferredLanguage,key);return <header className="topbar"><div><strong>{title||tr('Expoline Client Portal')}</strong><p>{tr('Manage companies, personnel and compliance.')}</p></div><div className="topbarActions"><LanguageToggle initial={user?.preferredLanguage||'en'}/><a className="profileButton" href="/notifications">🔔 {tr('Notifications')}</a></div></header>}