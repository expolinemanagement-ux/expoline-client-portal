import LanguageToggle from './LanguageToggle';import {getCurrentUser} from '@/lib/auth';export default async function Topbar(){const user=await getCurrentUser();return <header className="topbar"><div><strong>Expoline Client Portal</strong><p>Manage companies, personnel and compliance.</p></div><div className="topbarActions"><LanguageToggle initial={user?.preferredLanguage||'en'}/><a className="profileButton" href="/notifications">🔔 Notifications</a></div></header>