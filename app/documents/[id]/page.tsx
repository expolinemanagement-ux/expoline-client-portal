import Sidebar from '../../../components/Sidebar';import Topbar from '../../../components/Topbar';import DocumentForm from '../../../components/DocumentForm';import {prisma} from '../../../lib/prisma';import {notFound} from 'next/navigation';import {requireUser,canAccessCompany} from '../../../lib/auth';
export default async function DocumentDetail({params}:{params:Promise<{id:string}>}){const user=await requireUser();const {id}=await params;const doc=await prisma.document.findUnique({where:{id},include:{company:true,personnel:true}});if(!doc||!canAccessCompany(user,doc.companyId))return notFound();return <div className="appShell"><Sidebar/><main className="main"><Topbar/><section className="content"><div className="pageHeading"><div><h1>{doc.name}</h1><p>{doc.personnel?.fullName||'Company document'} · {doc.company.name}</p></div><a className="secondary linkButton" href="/documents">← Documents</a></div><section className="panel"><DocumentForm companies={[doc.company.name]} initial={doc} mode="edit"/></section><section className="panel"><h3>Stored file</h3><p><b>Type:</b> {doc.mimeType||'Unknown'} · <b>Size:</b> {doc.sizeBytes?`${doc.sizeBytes} bytes`:'Unknown'}</p><a className="primary linkButton" href={`/api/documents/${doc.id}/download`}>Download file</a><p className="muted">Files are kept outside the web app's public directory and downloads require an authenticated user with access to this company.</p></section></section></main></div>}