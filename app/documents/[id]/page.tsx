import Sidebar from '../../../components/Sidebar';import Topbar from '../../../components/Topbar';import DocumentForm from '../../../components/DocumentForm';import {prisma} from '../../../lib/prisma';import {notFound} from 'next/navigation';
export default async function DocumentDetail({params}:{params:{id:string}}){const [doc,companies]=await Promise.all([prisma.document.findUnique({where:{id:params.id},include:{company:true,personnel:true}}),prisma.company.findMany({select:{name:true},orderBy:{name:'asc'}})]);if(!doc)return notFound();return <div className="appShell"><Sidebar/><main className="main"><Topbar/><section className="content"><div className="pageHeading"><div><h1>{doc.name}</h1><p>{doc.personnel?.fullName||'Company document'} · {doc.company.name}</p></div><a className="secondary linkButton" href="/documents">← Documents</a></div><section className="panel"><DocumentForm companies={companies.map(c=>c.name)} initial={doc} mode="edit"/></section><section className="panel"><h3>Storage</h3><p><b>Storage key:</b> {doc.storageKey}</p><p><b>File type:</b> {doc.mimeType||'Not supplied'} · <b>Size:</b> {doc.sizeBytes?`${doc.sizeBytes} bytes`:'Not supplied'}</p><p className="muted">This demo currently stores document metadata only. Binary upload/download will be enabled after authentication and company-level access controls are in place.</p></section></section></main></div>}