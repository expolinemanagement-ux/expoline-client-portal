import Sidebar from '../../../components/Sidebar';import Topbar from '../../../components/Topbar';import DocumentForm from '../../../components/DocumentForm';import {prisma} from '../../../lib/prisma';
export default async function NewDocument(){const companies=await prisma.company.findMany({select:{name:true},orderBy:{name:'asc'}});return <div className="appShell"><Sidebar/><main className="main"><Topbar/><section className="content"><div className="pageHeading"><div><h1>Add Document</h1><p>Create a document record using demo metadata only.</p></div></div><section className="panel"><DocumentForm companies={companies.map(c=>c.name)}/></section></section></main></div>}