import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireUser,canAccessCompany} from '@/lib/auth';import {audit} from '@/lib/audit';
export async function GET(){try{const user=await requireUser();const personnel=await prisma.personnel.findMany({where:user.role==='SUPER_ADMIN'||user.role==='EXPOLINE_STAFF'?{}:{companyId:user.companyId||'__none__'},include:{company:true},orderBy:{fullName:'asc'}});return NextResponse.json(personnel)}catch{return NextResponse.json({error:'Unauthorized.'},{status:401})}}
export async function POST(request:Request){try{const user=await requireUser();const body=await request.json();if(!body.fullName?.trim()||!body.companyName?.trim())return NextResponse.json({error:'Full name and company are required.'},{status:400});const company=await prisma.company.findFirst({where:{name:body.companyName.trim()}});if(!company)return NextResponse.json({error:'Company not found.'},{status:404});if(!canAccessCompany(user,company.id))return NextResponse.json({error:'Company access denied.'},{status:403});const statusMap:Record<string,'CANDIDATE'|'RECRUITING'|'PROCESSING'|'ACTIVE'|'INACTIVE'>={Candidate:'CANDIDATE',Recruiting:'RECRUITING',Processing:'PROCESSING',Active:'ACTIVE',Inactive:'INACTIVE'};const personnel=await prisma.personnel.create({data:{companyId:company.id,fullName:body.fullName.trim(),chineseName:body.chineseName?.trim()||null,nationality:body.nationality?.trim()||null,position:body.position?.trim()||null,passportNumber:body.passportNumber?.trim()||null,passportExpiry:body.passportExpiry?new Date(body.passportExpiry):null,status:statusMap[body.status]??'CANDIDATE'},include:{company:true}});await audit(user.id,company.id,'CREATE','Personnel',personnel.id,{name:personnel.fullName});return NextResponse.json(personnel,{status:201})}catch(error){if(error instanceof Error&&error.message==='UNAUTHENTICATED')return NextResponse.json({error:'Unauthorized.'},{status:401});return NextResponse.json({error:'Unable to save personnel.'},{status:500})}}
