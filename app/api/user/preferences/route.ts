import {NextResponse} from 'next/server';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';export async function PUT(request:Request){try{const user=await requireUser();const {preferredLanguage}=await request.json();if(!['en','zh'].includes(preferredLanguage))return NextResponse.json({error:'Unsupported language.'},{status:400});await prisma.user.update({where:{id:user.id},data:{preferredLanguage}});return NextResponse.json({ok:true,preferredLanguage});}catch{return NextResponse.json({error:'Unable to save preference.'},{status:401});}}